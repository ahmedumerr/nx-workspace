import styles from './shared-components.module.scss';

export function SharedComponents() {
  return (
    <div className={styles['container']}>
      <h1>Welcome to SharedComponents!</h1>
    </div>
  );
}

export default SharedComponents;
